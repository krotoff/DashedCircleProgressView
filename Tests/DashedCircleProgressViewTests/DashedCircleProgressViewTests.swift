import XCTest
@testable import DashedCircleProgressView

final class DashedCircleProgressViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DashedCircleProgressView().text, "Hello, World!")
    }
}
